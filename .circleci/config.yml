version: 2

jobs:
  build-and-test:
    docker:
      - image: circleci/node:10.20.1
    steps:
      - checkout
      - run:
          name: Test
          command: python3 ./main.py

workflows:
  version: 2
  build:
    jobs:
      - build-and-test
